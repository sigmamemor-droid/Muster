-- Приватный сервер с доступом по коду
local Players = game:GetService("Players")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")

local SERVER_CODE = "12345" -- код доступа

-- Создаём GUI меню
local screenGui = Instance.new("ScreenGui")
screenGui.Parent = Players.LocalPlayer:WaitForChild("PlayerGui")

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 400, 0, 300)
frame.Position = UDim2.new(0.5, -200, 0.5, -150)
frame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
frame.BorderSizePixel = 0
frame.Parent = screenGui

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0.3, 0)
title.BackgroundTransparency = 1
title.Text = "ПРИВАТНЫЙ СЕРВЕР"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.TextScaled = true
title.Font = Enum.Font.GothamBold
title.Parent = frame

local codeLabel = Instance.new("TextLabel")
codeLabel.Size = UDim2.new(1, 0, 0.3, 0)
codeLabel.Position = UDim2.new(0, 0, 0.3, 0)
codeLabel.BackgroundTransparency = 1
codeLabel.Text = "Код: " .. SERVER_CODE
codeLabel.TextColor3 = Color3.fromRGB(100, 200, 255)
codeLabel.TextScaled = true
codeLabel.Font = Enum.Font.GothamMedium
codeLabel.Parent = frame

local joinButton = Instance.new("TextButton")
joinButton.Size = UDim2.new(0.6, 0, 0.25, 0)
joinButton.Position = UDim2.new(0.2, 0, 0.7, 0)
joinButton.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
joinButton.Text = "Создать / Присоединиться"
joinButton.TextColor3 = Color3.fromRGB(255, 255, 255)
joinButton.TextScaled = true
joinButton.Font = Enum.Font.GothamBold
joinButton.Parent = frame

joinButton.MouseButton1Click:Connect(function()
    local playerCode = SERVER_CODE -- здесь можно сделать поле ввода
    if playerCode == SERVER_CODE then
        TeleportService:Teleport(game.PlaceId, Players.LocalPlayer)
        print("Доступ разрешён. Сервер приватный.")
    else
        print("Неверный код!")
    end
end)

-- Защита от посторонних
game.Players.PlayerAdded:Connect(function(player)
    if player ~= Players.LocalPlayer then
        task.wait(2)
        player:Kick("Это приватный сервер. Доступ только по приглашению.")
    end
end)