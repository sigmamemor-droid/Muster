-- Compact left-side menu (safe UI) — place into StarterPlayerScripts
local Players = game:GetService("Players")
local Replicated = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local player = Players.LocalPlayer
if not player then Players:GetPropertyChangedSignal("LocalPlayer"):Wait(); player = Players.LocalPlayer end

-- ScreenGui
local gui = Instance.new("ScreenGui")
gui.Name = "CompactLeftMenu"
gui.ResetOnSpawn = false
gui.IgnoreGuiInset = true
gui.DisplayOrder = 50
gui.Parent = player:WaitForChild("PlayerGui")

-- Colors (compact/dark)
local bg = Color3.fromRGB(18,18,20)
local itemBg = Color3.fromRGB(26,26,28)
local hoverColor = Color3.fromRGB(40,40,44)
local accent = Color3.fromRGB(98,165,255)
local textColor = Color3.fromRGB(235,235,240)

-- Menu config (public icon decals)
local items = {
	{ id = "Home",     name = "Главная",   icon = "rbxassetid://9614426862"  }, -- Home icon
	{ id = "Chat",     name = "Чат",       icon = "rbxassetid://14714446"    }, -- Chat bubble
	{ id = "Store",    name = "Магазин",   icon = "rbxassetid://6031075933"  }, -- Shop/bag
	{ id = "Teleport", name = "Телепорт",  icon = "rbxassetid://10149699300" }, -- Teleport icon
	{ id = "Settings", name = "Настройки", icon = "rbxassetid://6031075938"  }, -- Gear/settings
}
local menuWidth = 58
local iconSize = 40
local spacing = 8

-- RemoteEvent (optional)
local event = Replicated:FindFirstChild("MenuClicked")
if not event then
	event = Instance.new("RemoteEvent")
	event.Name = "MenuClicked"
	event.Parent = Replicated
end

-- Container frame
local frame = Instance.new("Frame")
frame.Name = "LeftMenu"
frame.Size = UDim2.new(0, menuWidth, 0, (#items * (iconSize + spacing)) + spacing)
frame.Position = UDim2.new(0, 8, 0.5, -((#items * (iconSize + spacing)) + spacing)/2)
frame.BackgroundColor3 = bg
frame.BorderSizePixel = 0
frame.Parent = gui
Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 12)

local list = Instance.new("UIListLayout",