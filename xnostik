-- REDZ MOBILE v2
-- Полностью переработанный интерфейс

local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local Tween = game:GetService("TweenService")
local Run = game:GetService("RunService")
local LP = Players.LocalPlayer

-- Удаляем старый GUI
local old = LP.PlayerGui:FindFirstChild("RedzMobile")
if old then old:Destroy() end

local Screen = Instance.new("ScreenGui")
Screen.Name = "RedzMobile"
Screen.ResetOnSpawn = false
Screen.IgnoreGuiInset = true
Screen.Parent = LP.PlayerGui

-- Цвета
local C = {
    bg = Color3.fromRGB(13,13,18),
    card = Color3.fromRGB(22,22,30),
    accent = Color3.fromRGB(59,130,246),
    text = Color3.fromRGB(240,240,245),
    text2 = Color3.fromRGB(150,150,165),
    border = Color3.fromRGB(40,40,50)
}

-- Загрузочный экран
local Loader = Instance.new("Frame")
Loader.Size = UDim2.new(1,0,1,0)
Loader.BackgroundColor3 = C.bg
Loader.Parent = Screen

local Logo = Instance.new("TextLabel")
Logo.Size = UDim2.new(0,200,0,60)
Logo.Position = UDim2.new(0.5,-100,0.5,-30)
Logo.BackgroundTransparency = 1
Logo.Text = "REDZ"
Logo.TextColor3 = C.accent
Logo.TextSize = 42
Logo.Font = Enum.Font.GothamBlack
Logo.Parent = Loader

-- Анимация загрузки
Tween:Create(Logo, TweenInfo.new(1, Enum.EasingStyle.Quad), {TextTransparency = 1}):Play()
task.wait(1.2)
Tween:Create(Loader, TweenInfo.new(0.4), {BackgroundTransparency = 1}):Play()
task.wait(0.5)
Loader:Destroy()

-- Плавающая кнопка
local FloatBtn = Instance.new("TextButton")
FloatBtn.Size = UDim2.new(0,50,0,50)
FloatBtn.Position = UDim2.new(0,16,0.85,0)
FloatBtn.BackgroundColor3 = C.accent
FloatBtn.Text = "⚡"
FloatBtn.TextColor3 = Color3.new(1,1,1)
FloatBtn.TextSize = 22
FloatBtn.Font = Enum.Font.GothamBold
FloatBtn.Parent = Screen
Instance.new("UICorner", FloatBtn).CornerRadius = UDim.new(1,0)

-- Основное меню (нижняя панель)
local MenuOpen = false
local Main = Instance.new("Frame")
Main.Size = UDim2.new(1,0,0,0)
Main.Position = UDim2.new(0,0,1,0)
Main.BackgroundColor3 = C.bg
Main.ClipsDescendants = true
Main.Parent = Screen
Instance.new("UICorner", Main).CornerRadius = UDim.new(0,16)

-- Заголовок
local Header = Instance.new("Frame")
Header.Size = UDim2.new(1,0,0,44)
Header.BackgroundColor3 = C.card
Header.Parent = Main

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1,-80,1,0)
Title.Position = UDim2.new(0,16,0,0)
Title.BackgroundTransparency = 1
Title.Text = "REDZ v2"
Title.TextColor3 = C.text
Title.TextSize = 16
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Header

local Close = Instance.new("TextButton")
Close.Size = UDim2.new(0,36,0,36)
Close.Position = UDim2.new(1,-44,0,4)
Close.BackgroundTransparency = 1
Close.Text = "✕"
Close.TextColor3 = C.text2
Close.TextSize = 18
Close.Font = Enum.Font.GothamBold
Close.Parent = Header

-- Контент
local Content = Instance.new("Frame")
Content.Size = UDim2.new(1,0,1,-44)
Content.Position = UDim2.new(0,0,0,44)
Content.BackgroundTransparency = 1
Content.Parent = Main

local Pages = {}
local PageList = Instance.new("UIListLayout")
PageList.FillDirection = Enum.FillDirection.Horizontal
PageList.Parent = Content

-- Навигация внизу
local Nav = Instance.new("Frame")
Nav.Size = UDim2.new(1,0,0,56)
Nav.Position = UDim2.new(0,0,1,-56)
Nav.BackgroundColor3 = C.card
Nav.Parent = Main

local NavLayout = Instance.new("UIListLayout")
NavLayout.FillDirection = Enum.FillDirection.Horizontal
NavLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
NavLayout.Padding = UDim.new(0,20)
NavLayout.Parent = Nav

-- Функция создания страницы
local function CreatePage(name, icon)
    local page = Instance.new("ScrollingFrame")
    page.Size = UDim2.new(1,0,1,-56)
    page.BackgroundTransparency = 1
    page.ScrollBarThickness = 2
    page.Visible = false
    page.Parent = Content
    
    local layout = Instance.new("UIListLayout")
    layout.Padding = UDim.new(0,10)
    layout.Parent = page
    
    local pad = Instance.new("UIPadding")
    pad.PaddingTop = UDim.new(0,12)
    pad.PaddingLeft = UDim.new(0,14)
    pad.PaddingRight = UDim.new(0,14)
    pad.PaddingBottom = UDim.new(0,12)
    pad.Parent = page
    
    Pages[name] = page
    return page
end

-- Компоненты
local function AddSection(page, text)
    local l = Instance.new("TextLabel")
    l.Size = UDim2.new(1,0,0,24)
    l.BackgroundTransparency = 1
    l.Text = text
    l.TextColor3 = C.text2
    l.TextSize = 11
    l.Font = Enum.Font.GothamBold
    l.TextXAlignment = Enum.TextXAlignment.Left
    l.Parent = page
end

local function AddToggle(page, label, default, cb)
    local f = Instance.new("Frame")
    f.Size = UDim2.new(1,0,0,40)
    f.BackgroundColor3 = C.card
    f.Parent = page
    Instance.new("UICorner", f).CornerRadius = UDim.new(0,10)
    
    local t = Instance.new("TextLabel")
    t.Size = UDim2.new(1,-60,1,0)
    t.Position = UDim2.new(0,12,0,0)
    t.BackgroundTransparency = 1
    t.Text = label
    t.TextColor3 = C.text
    t.TextSize = 13
    t.Font = Enum.Font.GothamMedium
    t.TextXAlignment = Enum.TextXAlignment.Left
    t.Parent = f
    
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0,40,0,22)
    btn.Position = UDim2.new(1,-48,0.5,-11)
    btn.BackgroundColor3 = default and C.accent or Color3.fromRGB(50,50,60)
    btn.Text = ""
    btn.AutoButtonColor = false
    btn.Parent = f
    Instance.new("UICorner", btn).CornerRadius = UDim.new(1,0)
    
    local dot = Instance.new("Frame")
    dot.Size = UDim2.new(0,16,0,16)
    dot.Position = default and UDim2.new(1,-20,0.5,-8) or UDim2.new(0,4,0.5,-8)
    dot.BackgroundColor3 = Color3.new(1,1,1)
    dot.Parent = btn
    Instance.new("UICorner", dot).CornerRadius = UDim.new(1,0)
    
    local state = default
    btn.MouseButton1Click:Connect(function()
        state = not state
        Tween:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = state and C.accent or Color3.fromRGB(50,50,60)}):Play()
        Tween:Create(dot, TweenInfo.new(0.15), {Position = state and UDim2.new(1,-20,0.5,-8) or UDim2.new(0,4,0.5,-8)}):Play()
        if cb then cb(state) end
    end)
end

local function AddSlider(page, label, min, max, default, cb)
    local f = Instance.new("Frame")
    f.Size = UDim2.new(1,0,0,52)
    f.BackgroundColor3 = C.card
    f.Parent = page
    Instance.new("UICorner", f).CornerRadius = UDim.new(0,10)
    
    local t = Instance.new("TextLabel")
    t.Size = UDim2.new(0.7,0,0,20)
    t.Position = UDim2.new(0,12,0,4)
    t.BackgroundTransparency = 1
    t.Text = label
    t.TextColor3 = C.text
    t.TextSize = 13
    t.Font = Enum.Font.GothamMedium
    t.TextXAlignment = Enum.TextXAlignment.Left
    t.Parent = f
    
    local val = Instance.new("TextLabel")
    val.Size = UDim2.new(0.3,0,0,20)
    val.Position = UDim2.new(0.7,-12,0,4)
    val.BackgroundTransparency = 1
    val.Text = tostring(default)
    val.TextColor3 = C.accent
    val.TextSize = 13
    val.Font = Enum.Font.GothamBold
    val.TextXAlignment = Enum.TextXAlignment.Right
    val.Parent = f
    
    local track = Instance.new("TextButton")
    track.Size = UDim2.new(1,-24,0,4)
    track.Position = UDim2.new(0,12,0,34)
    track.BackgroundColor3 = Color3.fromRGB(50,50,60)
    track.Text = ""
    track.AutoButtonColor = false
    track.Parent = f
    Instance.new("UICorner", track).CornerRadius = UDim.new(1,0)
    
    local pct = (default - min) / (max - min)
    local fill = Instance.new("Frame")
    fill.Size = UDim2.new(pct,0,1,0)
    fill.BackgroundColor3 = C.accent
    fill.Parent = track
    Instance.new("UICorner", fill).CornerRadius = UDim.new(1,0)
    
    local thumb = Instance.new("Frame")
    thumb.Size = UDim2.new(0,14,0,14)
    thumb.Position = UDim2.new(pct,-7,0.5,-7)
    thumb.BackgroundColor3 = C.accent
    thumb.Parent = track
    Instance.new("UICorner", thumb).CornerRadius = UDim.new(1,0)
    
    local dragging = false
    local function update(input)
        local x = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
        local v = math.floor(min + (max - min) * x)
        fill.Size = UDim2.new(x,0,1,0)
        thumb.Position = UDim2.new(x,-7,0.5,-7)
        val.Text = tostring(v)
        if cb then cb(v) end
    end
    
    track.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            update(i)
        end
    end)
    UIS.InputChanged:Connect(function(i)
        if dragging and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
            update(i)
        end
    end)
    UIS.InputEnded:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)
end

-- Создаём страницы
local mainPage = CreatePage("Главная")
local combatPage = CreatePage("Бой")
local farmPage = CreatePage("Фарм")
local stylePage = CreatePage("Стиль")
local settingsPage = CreatePage("Настройки")

-- Заполняем
AddSection(mainPage, "Добро пожаловать")
AddToggle(mainPage, "Показать меню", true, function(s) end)

AddSection(combatPage, "Боевые опции")
AddToggle(combatPage, "Авто-аим", false, function(s) end)
AddToggle(combatPage, "Бесконечные патроны", true, function(s) end)

AddSection(farmPage, "Фарм")
AddToggle(farmPage, "Авто-фарм", false, function(s) end)
AddSlider(farmPage, "Скорость", 1, 10, 5, function(v) end)

AddSection(stylePage, "Визуал")
AddToggle(stylePage, "Крылья", false, function(s)
    -- твоя логика крыльев
end)
AddToggle(stylePage, "Аура", false, function(s)
    -- твоя логика ауры
end)
AddSlider(stylePage, "Размер", 1, 3, 1.5, function(v) end)

AddSection(settingsPage, "Игрок")
AddSlider(settingsPage, "Скорость ходьбы", 16, 120, 16, function(v)
    local h = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
    if h then h.WalkSpeed = v end
end)
AddSlider(settingsPage, "Сила прыжка", 50, 200, 50, function(v)
    local h = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
    if h then h.JumpPower = v end
end)
AddToggle(settingsPage, "Бесконечная выносливость", true, function(s) end)

-- Навигация
local tabs = {"Главная", "Бой", "Фарм", "Стиль", "Настройки"}
local icons = {"🏠", "⚔️", "⛏️", "✨", "⚙️"}
local navBtns = {}

for i, name in ipairs(tabs) do
    local b = Instance.new("TextButton")
    b.Size = UDim2.new(0,44,0,44)
    b.BackgroundTransparency = 1
    b.Text = icons[i] .. "\n" .. name
    b.TextColor3 = C.text2
    b.TextSize = 10
    b.Font = Enum.Font.GothamMedium
    b.AutoButtonColor = false
    b.Parent = Nav
    table.insert(navBtns, b)
    
    b.MouseButton1Click:Connect(function()
        for _, p in pairs(Pages) do p.Visible = false end
        Pages[name].Visible = true
        for _, btn in ipairs(navBtns) do
            btn.TextColor3 = C.text2
        end
        b.TextColor3 = C.accent
    end)
end

-- Показываем главную
Pages["Главная"].Visible = true
navBtns[1].TextColor3 = C.accent

-- Открытие/закрытие меню
FloatBtn.MouseButton1Click:Connect(function()
    MenuOpen = not MenuOpen
    Tween:Create(Main, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {
        Size = MenuOpen and UDim2.new(1,0,0.7,0) or UDim2.new(1,0,0,0),
        Position = MenuOpen and UDim2.new(0,0,0.3,0) or UDim2.new(0,0,1,0)
    }):Play()
end)

Close.MouseButton1Click:Connect(function()
    MenuOpen = false
    Tween:Create(Main, TweenInfo.new(0.3), {Size = UDim2.new(1,0,0,0), Position = UDim2.new(0,0,1,0)}):Play()
end)